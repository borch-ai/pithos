name: CodeQL

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '30 20 * * 1' # 20:30 on Mondays

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze-go:
    name: Analyze (Go)
    uses: borch-ai/.github/.github/workflows/codeql-with-sibling.yml@6fd0a9a05fa00d21159402147a3445af04a191c5 # v1.0.0
    with:
      sibling-repo: borch-ai/powerword
      sibling-path: powerword
      repo-path: pithos
    secrets: inherit
