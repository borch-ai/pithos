name: CI

on:
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  pull-requests: read

jobs:
  run:
    uses: borch-ai/.github/.github/workflows/go-ci-with-sibling.yml@main
    secrets: inherit
    with:
      sibling-repo: borch-ai/powerword
      sibling-path: powerword
      repo-path: .
