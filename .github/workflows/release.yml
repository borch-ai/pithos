name: Release

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  release:
    uses: borch-ai/.github/.github/workflows/release-go-binary.yml@main
    with:
      binary-name: pithos
      sibling-repo: borch-ai/powerword
      sibling-path: powerword
    secrets: inherit
