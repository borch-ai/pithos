name: Release

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  release:
    uses: borch-ai/.github/.github/workflows/release-go-binary.yml@v0.2.2
    with:
      binary-name: pithos
      sibling-repo: borch-ai/powerword
      sibling-path: powerword
    secrets: inherit
