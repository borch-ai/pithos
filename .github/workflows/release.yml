name: Release

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  release:
    uses: borch-ai/.github/.github/workflows/release-go-binary.yml@c4337f330451690c3bbbffe61d9cc063d401183e # pin@v0.0.6
    with:
      binary-name: pithos
      sibling-repo: borch-ai/powerword
      sibling-path: powerword
    secrets: inherit
